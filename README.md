# BreederDao Test
This is for an exam for BreaderDao.

# Installation
1. Install packages
    ```
    npm i
    ```
2. Build package
    ```
    npm run dev
    ```
3. View in browser - http://localhost:8000/
